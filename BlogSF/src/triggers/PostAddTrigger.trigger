trigger PostAddTrigger on Post__c (before insert, before update) {
	
	/*Post__c post = Trigger.New[0];
	String postID;

	if(Trigger.isInsert){
		insert post;
		postID = post.Id;

	//consulta para obtener todos los tags
	//		si un tags esta en tagsName
	//			crea un objeto de tipo jobPostTags
	//				inserta el objeto con el id del tag y el id del post
	//		Si el tags no esta
	//			cre un tags insertalo y obten el id
	//				crea un objeto de tipo jobPostTags
	//					inserta el objeto con el id del tag y el id del post//			


	}
	if(Trigger.isUpdate){				
		postID = post.Id;
		update post;


	//consulta para obtener todos los tags
	//		si un tags esta en tagsName
	//			crea un objeto de tipo jobPostTags
	//				inserta el objeto con el id del tag y el id del post
	//		Si el tags no esta
	//			cre un tags insertalo y obten el id
	//				crea un objeto de tipo jobPostTags
	//					inserta el objeto con el id del tag y el id del post//			

	}

	String stringTags = post.Tag__c;	
	List<String> tagsName = stringTags.split(',');
	
	*/

// listo !!!!!!!!

}